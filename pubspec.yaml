name: tech_clout
description: Eswatini Technology Talks Social Network.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.0.0
  firebase_core: ^1.1.0
  cloud_firestore: ^2.0.0
  firebase_auth: ^1.1.3
  provider: ^5.0.0
  image_picker: ^0.7.5
  flutter_spinkit: ^5.0.0
  email_validator: ^2.0.1
  image_cropper: ^1.4.0
  firebase_storage: ^8.0.6
  flutter_pw_validator: ^1.2.1
  progress_dialog: ^1.2.4
  intl: ^0.17.0
  timeago: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/comment.png
    - assets/images/forgot.png
    - assets/images/login.png
    - assets/images/logo.png
    - assets/images/guy.png
